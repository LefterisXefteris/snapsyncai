# Product facts rules live only on the server

Confirm, the listing copy generate gate, description-block HTML, and stale listing copy are decided in the FastAPI Product facts module. The SPA does not re-encode those rules. HTTP returns outcomes; form chrome may keep pick labels and unsaved draft fields.

We rejected a generated TypeScript twin (still two implementations unless the generator is never edited) and a shared package (a packaging seam for in-process rules that only need one runtime).
