---
phase: 10-pricing-model-update
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - client/src/pages/Landing.tsx
autonomous: false

must_haves:
  truths:
    - "Landing page CREDIT_PACKS array shows £4.50, £17.50, £39.50 (halved prices)"
    - "Landing page perCredit strings are updated to reflect the new per-credit costs"
    - "Landing page FAQ answer for 'How much does SnapSync AI cost?' mentions both the subscription plans (£9/mo, £79/yr) and the new credit pack prices"
    - "Landing page JSON-LD offers array reflects the new credit pack prices"
    - "Landing page pricing section copy no longer says 'No subscriptions' — it acknowledges the subscription option exists"
    - "A subscription pricing section or cards appear on the landing page showing £9/month and £79/year"
  artifacts:
    - path: "client/src/pages/Landing.tsx"
      provides: "Updated CREDIT_PACKS, subscription section/cards, updated FAQ, updated JSON-LD offers"
      contains: "£4.50"
  key_links:
    - from: "client/src/pages/Landing.tsx subscription card button"
      to: "openSignIn()"
      via: "onClick handler sending user to sign up"
      pattern: "openSignIn"
---

<objective>
Update Landing.tsx to reflect the new pricing model: halve the displayed credit pack prices, add a subscription pricing option (£9/month and £79/year), update the FAQ pricing answer to mention both subscription and credit options, update the JSON-LD structured data, and remove the "No subscriptions" copy that is now incorrect.

Purpose: The landing page is the primary acquisition surface. If it shows stale prices or denies subscriptions exist, new visitors will be misled and existing subscribers will be confused.
Output: Landing page with accurate new credit prices, a visible subscription section, and a corrected FAQ.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-pricing-model-update/10-CONTEXT.md
@.planning/phases/10-pricing-model-update/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Landing.tsx — credit pack prices, JSON-LD, FAQ, and subscription section</name>
  <files>client/src/pages/Landing.tsx</files>
  <action>
Make the following changes to Landing.tsx:

### 1. Update CREDIT_PACKS array (lines 73–105)

Update the price and perCredit fields:
```typescript
const CREDIT_PACKS = [
  {
    id: "starter",
    name: "Starter",
    credits: 10,
    price: "£4.50",           // was £9
    description: "Perfect for trying it out",
    perCredit: "45p per product",  // was 90p
    highlight: false,
    // ... keep everything else (badge, features array) unchanged
  },
  {
    id: "growth",
    name: "Growth",
    credits: 50,
    price: "£17.50",          // was £35
    description: "Best for regular sellers",
    perCredit: "35p per product",  // was 70p
    // ... keep everything else unchanged
  },
  {
    id: "pro",
    name: "Pro",
    credits: 150,
    price: "£39.50",          // was £79
    description: "For high-volume stores",
    perCredit: "26p per product",  // was 53p
    // ... keep everything else unchanged
  },
];
```
Only change the `price` and `perCredit` fields. Do NOT change id, name, credits, description, highlight, badge, or features arrays.

### 2. Update softwareJsonLd offers array (around lines 240–244)

Change:
```typescript
"offers": [
  { "@type": "Offer", "price": "9.00", "priceCurrency": "GBP", "name": "Starter — 10 credits" },
  { "@type": "Offer", "price": "35.00", "priceCurrency": "GBP", "name": "Growth — 50 credits" },
  { "@type": "Offer", "price": "79.00", "priceCurrency": "GBP", "name": "Pro — 150 credits" },
],
```
To:
```typescript
"offers": [
  { "@type": "Offer", "price": "9.00", "priceCurrency": "GBP", "name": "Monthly subscription — unlimited", "priceSpecification": { "@type": "UnitPriceSpecification", "price": "9.00", "priceCurrency": "GBP", "billingIncrement": 1, "unitCode": "MON" } },
  { "@type": "Offer", "price": "79.00", "priceCurrency": "GBP", "name": "Annual subscription — unlimited", "priceSpecification": { "@type": "UnitPriceSpecification", "price": "79.00", "priceCurrency": "GBP", "billingIncrement": 1, "unitCode": "ANN" } },
  { "@type": "Offer", "price": "4.50", "priceCurrency": "GBP", "name": "Starter — 10 credits" },
  { "@type": "Offer", "price": "17.50", "priceCurrency": "GBP", "name": "Growth — 50 credits" },
  { "@type": "Offer", "price": "39.50", "priceCurrency": "GBP", "name": "Pro — 150 credits" },
],
```

### 3. Update the pricing section heading/subtext (around lines 628–633)

The current copy says "Pay Once. Credits Never Expire." and "No subscriptions. No monthly charges." This is now incorrect since subscriptions exist. Update to acknowledge both options:

Change the `<h2>` text from:
```
Pay Once. Credits Never Expire.
```
To:
```
Simple, Transparent Pricing
```

Change the `<p>` subtext from:
```
No subscriptions. No monthly charges. Buy credits when you need them — each credit unlocks full AI analysis for one product.
```
To:
```
Subscribe for unlimited access, or buy credit packs and pay only for what you use. No hidden fees, no surprises.
```

Change the badge pill copy (line 634–637) from:
```
1 credit = 1 product (all views/angles included)
```
To:
```
Credits never expire · Cancel subscription anytime
```

### 4. Add subscription cards ABOVE the credit packs grid

Immediately before the `{/* Free tier + Credit packs */}` comment (around line 640), insert a subscription section:

```tsx
{/* Subscription plans */}
<div className="grid grid-cols-1 sm:grid-cols-2 gap-6 mb-10 max-w-2xl mx-auto">
  {/* Monthly */}
  <Card className="flex flex-col reveal delay-1 bg-background/50 backdrop-blur-md border border-white/10 hover:border-white/20 transition-all duration-300 shadow-xl">
    <CardHeader className="pb-4">
      <div className="flex items-center gap-2 mb-1">
        <Crown className="w-4 h-4 text-primary" />
        <CardTitle className="text-xl">Pro Monthly</CardTitle>
      </div>
      <CardDescription>Unlimited access, billed monthly</CardDescription>
      <div className="pt-3">
        <span className="text-4xl font-display font-bold">£9</span>
        <span className="text-muted-foreground text-sm ml-2">/month</span>
      </div>
    </CardHeader>
    <CardContent className="flex-1 flex flex-col">
      <Separator className="mb-5 border-white/10" />
      <ul className="space-y-3 flex-1 mb-6">
        {["Unlimited AI analysis", "Full descriptions & pricing", "SEO & AEO content", "Push to all stores", "Cancel anytime"].map((item, i) => (
          <li key={i} className="flex items-start gap-2.5 text-sm">
            <CheckCircle2 className="w-4 h-4 text-muted-foreground mt-0.5 flex-shrink-0" />
            <span className="text-muted-foreground">{item}</span>
          </li>
        ))}
      </ul>
      <Button variant="outline" className="w-full mt-auto rounded-xl border-border/60 hover:bg-primary/5 hover:border-primary/50 transition-all duration-300" onClick={() => openSignIn()}>
        Get Started — £9/mo
      </Button>
    </CardContent>
  </Card>

  {/* Annual */}
  <Card className="flex flex-col relative overflow-hidden reveal delay-2 border-primary/50 shadow-[0_0_50px_rgba(16,185,129,0.15)] bg-background/60 backdrop-blur-xl hover:shadow-[0_0_60px_rgba(16,185,129,0.25)] transition-all duration-300 shadow-xl">
    <div className="absolute inset-0 bg-gradient-to-b from-primary/10 to-transparent opacity-50 pointer-events-none" />
    <div className="absolute top-0 inset-x-0 h-1 bg-gradient-to-r from-lime-500 via-emerald-500 to-primary" />
    <CardHeader className="pb-4 relative z-10">
      <div className="flex items-center justify-between gap-2 mb-1">
        <div className="flex items-center gap-2">
          <Crown className="w-4 h-4 text-primary" />
          <CardTitle className="text-xl">Pro Annual</CardTitle>
        </div>
        <Badge className="text-[10px]">Best value</Badge>
      </div>
      <CardDescription>Unlimited access, save 2 months</CardDescription>
      <div className="pt-3">
        <span className="text-4xl font-display font-bold">£79</span>
        <span className="text-muted-foreground text-sm ml-2">/year</span>
      </div>
      <p className="text-xs text-primary mt-1 font-medium">= £6.58/mo · 2 months free</p>
    </CardHeader>
    <CardContent className="flex-1 flex flex-col relative z-10">
      <Separator className="mb-5" />
      <ul className="space-y-3 flex-1 mb-6">
        {["Unlimited AI analysis", "Full descriptions & pricing", "SEO & AEO content", "Push to all stores", "Best per-month rate"].map((item, i) => (
          <li key={i} className="flex items-start gap-2.5 text-sm">
            <CheckCircle2 className="w-4 h-4 text-primary mt-0.5 flex-shrink-0" />
            <span>{item}</span>
          </li>
        ))}
      </ul>
      <Button className="w-full mt-auto rounded-xl shadow-md shadow-primary/20" onClick={() => openSignIn()}>
        Get Started — £79/yr
        <ArrowRight className="w-4 h-4 ml-2" />
      </Button>
    </CardContent>
  </Card>
</div>

<div className="text-center mb-8 reveal">
  <p className="text-sm text-muted-foreground">Or buy credits and pay only for what you use</p>
</div>
```

Import `Crown` from lucide-react if not already imported (check the existing import line at the top of Landing.tsx and add `Crown` if missing).

### 5. Update the bottom tagline (line 722–724)

Change:
```
Credits never expire · Secure payments via Stripe · No subscriptions, no surprises
```
To:
```
Credits never expire · Cancel subscription anytime · Secure payments via Stripe
```

### 6. Update the FAQ answer for pricing (around lines 128–131)

Change the answer for "How much does SnapSync AI cost?":
From:
```
"Uploading images and getting AI previews is completely free — no credit card required. To unlock full AI-generated descriptions, pricing, SEO, AEO content, and variants, buy a credit pack: Starter (10 credits / £9), Growth (50 credits / £35), or Pro (150 credits / £79). Credits never expire — pay once, use whenever."
```
To:
```
"Uploading images and getting AI previews is completely free — no credit card required. To unlock full AI-generated descriptions, pricing, SEO, AEO content, and variants, choose a plan: subscribe for £9/month or £79/year for unlimited access, or buy a one-time credit pack — Starter (10 credits / £4.50), Growth (50 credits / £17.50), or Pro (150 credits / £39.50). Credits never expire."
```

Also update the FAQ answer for "How do credits work?" (around line 113–115) to acknowledge subscriptions:
From:
```
"Each credit unlocks full AI analysis for one product. A product can have multiple images (e.g. front, back, detail shots) — they all count as 1 credit. Buy a credit pack once and use them whenever you need — credits never expire. No subscriptions, no recurring charges."
```
To:
```
"Each credit unlocks full AI analysis for one product. A product can have multiple images (e.g. front, back, detail shots) — they all count as 1 credit. Buy a credit pack once and use them whenever you need — credits never expire. Alternatively, subscribe for unlimited access with no per-product charges."
```
  </action>
  <verify>
    1. `npx tsc --noEmit` — no TypeScript errors
    2. `grep -n "£4.50\|£17.50\|£39.50" client/src/pages/Landing.tsx` — shows updated credit pack prices
    3. `grep -n "£9.*month\|£79.*year\|Pro Monthly\|Pro Annual" client/src/pages/Landing.tsx` — shows subscription cards
    4. `grep -n "No subscriptions" client/src/pages/Landing.tsx` — returns no hits (old copy removed)
    5. `grep -n "Crown" client/src/pages/Landing.tsx` — shows Crown is imported
  </verify>
  <done>
    - Landing.tsx CREDIT_PACKS shows £4.50, £17.50, £39.50 with updated perCredit strings
    - Subscription section with Pro Monthly (£9/mo) and Pro Annual (£79/yr) cards is rendered above credit packs
    - JSON-LD offers includes subscription entries and updated credit pack prices
    - FAQ pricing answer updated to mention £9/mo, £79/yr, and new credit pack prices
    - "No subscriptions" copy is gone from pricing section
    - TypeScript compiles cleanly
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 10 pricing model update across all 3 plans:
    - Plan 01: Server routes updated (new price constants, dual checkout, migration/archive endpoints) + hook types
    - Plan 02: Sidebar subscribe dialog with monthly/annual toggle; Home.tsx fallback prices corrected
    - Plan 03: Landing.tsx with subscription section, new credit pack prices, updated FAQ and JSON-LD
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`

    2. **Sidebar — subscribe dialog:**
       - Open the app, click "Subscribe" in the left sidebar
       - Dialog should show TWO toggle cards: "£9/mo Monthly" and "£79/yr Annual · save 2 months"
       - Clicking each card should highlight it
       - Confirm button label should update to reflect selection

    3. **Sidebar — subscribe flow (monthly):**
       - Select Monthly, click Subscribe
       - Should redirect to Stripe Checkout with a recurring monthly price of £9
       - Verify session shows "£9.00 / month" in Stripe test checkout

    4. **Sidebar — subscribe flow (annual):**
       - Select Annual, click Subscribe
       - Should redirect to Stripe Checkout with a recurring annual price of £79
       - Verify session shows "£79.00 / year" in Stripe test checkout

    5. **Credits dialog (Home page):**
       - Open the credits purchase dialog
       - Confirm prices show £4.50 (Starter), £17.50 (Growth), £39.50 (Pro)
       - (If paymentConfig is loading, the fallback values should also show the new prices — test with network throttling)

    6. **Landing page — subscription section:**
       - Navigate to http://localhost:5173 (or wherever dev runs)
       - Scroll to pricing section
       - Should see "Pro Monthly £9/mo" and "Pro Annual £79/yr" cards
       - Should NOT see "No subscriptions" text anywhere in the pricing area

    7. **Landing page — credit pack prices:**
       - Credit pack cards should show £4.50, £17.50, £39.50
       - perCredit values: 45p, 35p, 26p

    8. **Landing page — FAQ:**
       - "How much does SnapSync AI cost?" answer should mention £9/month, £79/year, and the new credit pack prices

    9. **API spot-check:**
       - `curl http://localhost:PORT/api/payments/config` (unauthenticated GET)
       - Response should include `subscriptionMonthlyPricePence: 900`, `subscriptionAnnualPricePence: 7900`
       - Credit packs should have pricePence: 450, 1750, 3950
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Describe any issues found to trigger gap closure.</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `grep -n "No subscriptions" client/src/pages/Landing.tsx` returns no hits
3. `grep -c "£4.50\|£17.50\|£39.50" client/src/pages/Landing.tsx` returns 3+ hits
4. `grep -n "Pro Monthly\|Pro Annual" client/src/pages/Landing.tsx` shows subscription card section
</verification>

<success_criteria>
- Landing page pricing section shows both subscription plans and halved credit packs
- All six phase success criteria are achievable after Plans 01–03 are executed
- Human verification confirms Stripe checkout creates correct sessions for monthly and annual intervals
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-pricing-model-update/10-03-SUMMARY.md`
</output>
