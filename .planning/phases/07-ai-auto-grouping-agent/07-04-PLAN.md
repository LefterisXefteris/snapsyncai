---
phase: 07-ai-auto-grouping-agent
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified: []
autonomous: false
requirements: [GROUP-01, GROUP-02, GROUP-03, GROUP-04]

must_haves:
  truths:
    - "All four success criteria from the phase are verified by a human"
    - "Auto-grouping works end-to-end with real images"
    - "Manual mode is unbroken"
---

<objective>
Human verification of the complete AI auto-grouping flow.

Purpose: Confirm all phase success criteria are met with real usage.
Output: Human-verified phase completion
</objective>

<execution_context>
@/Users/lefterisgilmaz/Desktop/lisai-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lefterisgilmaz/Desktop/lisai-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete AI auto-grouping agent: SSE vision endpoint, useAutoGroup hook, mode choice UI, live group streaming, label display, confirm & analyze flow, and manual mode preservation.</what-built>
  <how-to-verify>
**Pre-requisite:** Start the dev server (`npm run dev`).

**Test 1: Mode choice appears (GROUP-04)**
1. Navigate to the upload page
2. Drop 5+ images of different products
3. Verify you see two buttons: "Auto-group with AI" and "Group manually"
4. Click "Group manually" — verify the existing drag-and-drop flow works normally

**Test 2: AI auto-grouping (GROUP-01)**
1. Drop 5-10 images that include multiple photos of 2-3 different products (e.g., same shirt from different angles, a pair of shoes, a bag)
2. Click "Auto-group with AI"
3. Verify: groups appear one by one as the AI processes (not all at once after a delay)
4. Verify: each group card shows a product label (e.g., "Blue Denim Jacket") with a confidence badge

**Test 3: Review and tweak (GROUP-02)**
1. After auto-grouping completes, check the summary banner ("AI identified N products")
2. Try dragging an image from one group to another — verify it moves correctly
3. Try splitting a group — verify it breaks into individual images
4. Verify: "Switch to manual" button works — clears AI groupings and enters manual mode

**Test 4: Confirm & Analyze (GROUP-03)**
1. After auto-grouping, click "Confirm & Analyze All"
2. Verify: progress bar shows analysis progress per product group
3. Verify: after completion, toast notification appears
4. Verify: products appear in the gallery with AI-generated titles and descriptions

**Test 5: Edge cases**
1. Drop only 1 image — verify mode choice still works (manual should work, auto should handle gracefully)
2. Cancel auto-grouping mid-progress — verify it stops and you can try again or switch to manual
3. Remove all images during mode choice — verify UI resets cleanly
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All four phase success criteria verified by human testing with real images.
</verification>

<success_criteria>
Human confirms: (1) AI auto-grouping detects and groups same-product images, (2) groupings are reviewable and editable, (3) confirming triggers full AI analysis per group, (4) manual mode still works as alternative.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-auto-grouping-agent/07-04-SUMMARY.md`
</output>
