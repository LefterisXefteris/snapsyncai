---
phase: 07-ai-auto-grouping-agent
plan: 05
type: execute
wave: 5
depends_on: ["07-03"]
files_modified:
  - shared/routes.ts
  - server/auto-group-utils.ts
  - server/routes.ts
  - client/src/hooks/use-auto-group.ts
  - client/src/components/upload-zone.tsx
autonomous: true
requirements: [GROUP-01, GROUP-02, GROUP-03, GROUP-04]

must_haves:
  truths:
    - "Workspace exposes a simple one-click action to sort uploaded images into product families"
    - "Same product in different colors, sizes, materials, prints, washes, and camera angles can be grouped under one product"
    - "Variant-family grouping remains reviewable and editable in the existing staging UI"
    - "Manual mode remains available and unchanged as a fallback"
  artifacts:
    - path: "server/auto-group-utils.ts"
      provides: "Variant-family normalization and merge rules for auto-grouping"
    - path: "server/routes.ts"
      provides: "Auto-group endpoint behavior for variant-aware product-family grouping"
    - path: "client/src/hooks/use-auto-group.ts"
      provides: "Client trigger for variant-aware grouping mode"
    - path: "client/src/components/upload-zone.tsx"
      provides: "Simple workspace button for sorting variants into products"
  key_links:
    - from: "client/src/components/upload-zone.tsx"
      to: "client/src/hooks/use-auto-group.ts"
      via: "startGrouping with explicit grouping mode"
      pattern: "variant-family sort button"
    - from: "client/src/hooks/use-auto-group.ts"
      to: "/api/images/auto-group"
      via: "request body mode field"
      pattern: "variant-aware auto-group request"
    - from: "server/routes.ts"
      to: "server/auto-group-utils.ts"
      via: "prompt construction + deterministic family merge"
      pattern: "variant-family consolidation"
---

<objective>
Add a simple workspace button that agentically sorts uploaded images into product families, grouping same-product variants under one product while preserving the existing review-and-tweak flow.

Purpose: Users with large variant-heavy uploads should be able to go from hundreds of images to the true underlying product count with a single obvious action instead of manual regrouping.
Output: A variant-aware grouping mode exposed by a simple button in the upload workspace, backed by stronger product-family grouping on the server.
</objective>

<execution_context>
@/Users/lefterisgilmaz/Desktop/lisai-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lefterisgilmaz/Desktop/lisai-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-auto-grouping-agent/07-CONTEXT.md

@shared/routes.ts
@server/auto-group-utils.ts
@server/routes.ts
@client/src/hooks/use-auto-group.ts
@client/src/components/upload-zone.tsx

<decisions>
- The workspace must expose a simple, explicit button for "sort variants into products"
- Same base product in different colors, sizes, materials, prints, washes, and views should collapse into one product family
- If the model is genuinely unsure, prefer separate groups over destructive over-merging
- Users still review and tweak results in the existing grouping UI
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit variant-family grouping mode to the auto-group contract</name>
  <files>shared/routes.ts, client/src/hooks/use-auto-group.ts</files>
  <action>
Update the client/server contract so auto-grouping can be requested in an explicit "variant-family" mode rather than relying only on a generic AI grouping action.

**In `client/src/hooks/use-auto-group.ts`:**
- Extend `startGrouping` to accept an optional mode argument, e.g. `"default"` vs `"variant-family"`
- Include that mode in the POST body to `/api/images/auto-group`
- Keep the existing default behavior backward-compatible so current callers still work

**In `shared/routes.ts`:**
- Document the auto-group route request shape in code comments or adjacent typing so the mode is discoverable to future callers

**Behavior target:**
- Existing "Auto-group with AI" can keep using the default mode
- The new workspace button will call the stronger `"variant-family"` mode explicitly
  </action>
  <verify>
    <automated>cd /Users/lefterisgilmaz/Desktop/lisai-app && pnpm test</automated>
  </verify>
  <done>The hook can trigger variant-aware grouping explicitly without breaking current callers.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen server-side grouping around product families, not variant labels</name>
  <files>server/auto-group-utils.ts, server/routes.ts</files>
  <action>
Refine the auto-group endpoint so "variant-family" mode aggressively groups same-product variants under one product while still avoiding reckless merges.

**In `server/auto-group-utils.ts`:**
- Separate default grouping prompt behavior from variant-family grouping prompt behavior if needed
- Ensure canonical family normalization strips color/view/size-specific words from labels
- Add deterministic merge behavior keyed on family identity so near-duplicate labels collapse into one product family
- Keep confidence escalation deterministic when merging families

**In `server/routes.ts`:**
- Read the new grouping mode from the request body
- Use the variant-family prompt/merge path when mode is `"variant-family"`
- Preserve the existing SSE response shape so the client review UI does not need a rewrite

**Important constraints:**
- Batch behavior and streaming should remain intact
- Every image index must still appear exactly once in the final response
- When uncertain, favor leaving families separate instead of silently merging unrelated products
  </action>
  <verify>
    <automated>cd /Users/lefterisgilmaz/Desktop/lisai-app && pnpm test</automated>
    <manual>Spot-check the prompt and merge logic against examples like "Blue Denim Jacket" + "Black Denim Jacket" + back/detail views.</manual>
  </verify>
  <done>The endpoint can explicitly run a variant-family grouping pass that collapses same-product variants into one product family.</done>
</task>

<task type="auto">
  <name>Task 3: Add the simple workspace button and preserve reviewability</name>
  <files>client/src/components/upload-zone.tsx</files>
  <action>
Add an obvious workspace action that tells the agent to sort uploaded images into product families with one click.

**UX requirements:**
- Add a button label that is outcome-oriented, e.g. "Sort Variants Into Products"
- Place it where users already make grouping decisions, not buried in secondary controls
- The button should trigger `startGrouping(..., "variant-family")`
- The resulting groups must still land in the same editable review UI (drag, split, manual cleanup)

**Interaction expectations:**
- Users can still choose manual grouping
- Existing auto-group action can remain if it serves a different/default grouping behavior
- The new button should feel like the fastest path for large variant-heavy uploads

**Microcopy goal:**
- Make it obvious that the result is one product family with variants grouped together, not one giant merged product containing unrelated items
  </action>
  <verify>
    <manual>In the workspace, drop a mixed batch and confirm the new button is visible, understandable, and routes into the existing review flow.</manual>
  </verify>
  <done>The upload workspace exposes a simple one-click variant-family grouping action that feeds into the existing editable grouping UI.</done>
</task>

<task type="auto">
  <name>Task 4: Add regression coverage for variant-family grouping behavior</name>
  <files>tests/auto-group-utils.test.ts, tests/upload-langgraph.test.ts</files>
  <action>
Extend automated coverage so the new product-family behavior is pinned down and future changes do not regress it.

**Add tests for:**
- Variant-family normalization removes color/view/size-specific wording
- Same-product variant labels merge into one family in variant-aware mode
- Distinct products remain separate
- Request mode wiring from client contract does not break existing defaults

If `tests/upload-langgraph.test.ts` is not the right place for contract coverage, create a new focused test file instead. Prefer narrow, deterministic tests over UI-heavy test scaffolding.
  </action>
  <verify>
    <automated>cd /Users/lefterisgilmaz/Desktop/lisai-app && pnpm test</automated>
  </verify>
  <done>Automated tests cover the new variant-family grouping behavior and keep existing upload behavior green.</done>
</task>

</tasks>

<verification>
The workspace offers a simple one-click variant-family grouping action, the server groups same-product variants under one product family more reliably, and the resulting groups remain editable before analysis.
</verification>

<success_criteria>
1. Users can click a simple workspace button to sort uploaded images into product families
2. Same base product in different colors, sizes, materials, prints, washes, and views is grouped under one product when visually appropriate
3. Users can still review, split, drag, and manually correct the results before confirming
4. Existing manual grouping flow remains intact
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-auto-grouping-agent/07-05-SUMMARY.md`
</output>
