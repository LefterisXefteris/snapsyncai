---
phase: 09-manual-grouping-first-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - GROUP-05
  - GROUP-06
  - GROUP-07
  - GROUP-08
  - GROUP-09
  - GROUP-10
  - GROUP-11
  - GROUP-12
must_haves:
  truths:
    - "REQUIREMENTS.md defines GROUP-05..12 under a Phase 9 section"
    - "ROADMAP.md Phase 9 entry has a concrete Goal (not placeholder) and Success Criteria"
    - "Every GROUP-05..12 ID appears in the Traceability table mapped to Phase 9"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "GROUP-05..12 requirement definitions + traceability rows"
      contains: "GROUP-05"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 concrete Goal, Depends on, Requirements, Success Criteria"
      contains: "Phase 9: Manual Grouping-First UX"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Requirements: line lists GROUP-05..12"
      pattern: "Requirements.*GROUP-05"
---

<objective>
Finalize Phase 9 traceability: add GROUP-05..12 to REQUIREMENTS.md and replace the Phase 9 placeholders in ROADMAP.md with a concrete Goal, Requirements list, and Success Criteria.

Purpose: Unblock downstream plans (02-05) by giving every plan a requirement ID to reference in its frontmatter. Without this, traceability checks fail.
Output: REQUIREMENTS.md and ROADMAP.md updates. No code changes.
</objective>

<execution_context>
@/Users/lefterisgilmaz/Desktop/lisai-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lefterisgilmaz/Desktop/lisai-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/09-manual-grouping-first-ux/09-CONTEXT.md
@.planning/phases/09-manual-grouping-first-ux/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GROUP-05..12 to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Under the "Product UX Requirements" section (after the "Embeddings Variant Clustering (Phase 8)" block and before "Out of Scope"), add a new subsection titled:

### Manual Grouping-First UX (Phase 9)

Populate it with eight unchecked checkboxes matching the RESEARCH.md "Phase Requirements" table verbatim (bullets in bold ID format matching GROUP-01..04 style):

- GROUP-05: Drag-and-drop staging is the primary grouping UX; the mode chooser ("Auto-group / Sort Variants / Group manually") is removed, manual mode starts immediately on file drop.
- GROUP-06: The "Custom AI Prompt" textarea, brand-tone selector, and productContext / brandTone state are permanently deleted from upload-zone.tsx; the upload call stops sending them.
- GROUP-07: Shift-click selects a range within/across groups in visual order, Cmd/Ctrl-click toggles a single thumbnail, plain click selects only that thumbnail; multi-select persists across a drag.
- GROUP-08: The per-group maxImages counter, the +/− controls, and the PRESETS = [1..5] toolbar are removed; groups larger than LARGE_GROUP_THRESHOLD (20) show a soft warning badge but no hard cap.
- GROUP-09: An invalid drop (over === null) animates the dragged thumbnail back to its origin using dnd-kit's built-in dropAnimation instead of vanishing instantly.
- GROUP-10: Confirming the staged layout pushes each group to Supabase by calling POST /api/images/upload with groupAsOne=true once per group (CONCURRENCY=2); on per-group success the IDB blobs are cleared for that group, on per-group failure the failed group remains in staging with a retry affordance.
- GROUP-11: The AI auto-group / "Sort variants" flow remains accessible as a secondary toolbar button (not hidden) but is no longer the landing UX; useAutoGroup and the Phase 8 fallback banner continue to work unchanged.
- GROUP-12: A "+ New group" drop target is always visible at the end of the group grid so users can create an empty group by dropping onto it.

Then append eight rows to the Traceability table at the bottom of the file, each mapping `GROUP-05` through `GROUP-12` to `Phase 9` with status `Pending`.

Update the "Coverage:" footer:
- Product UX requirements: 16 total (was 8)
- Mapped to phases: 24 (was 16)

Update the bottom dateline: `*Last updated: 2026-04-11 — added GROUP-05 through GROUP-12 for Phase 9*`
  </action>
  <verify>
    <automated>grep -cE "GROUP-(0[5-9]|1[0-2])" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
REQUIREMENTS.md contains all eight GROUP-05..12 IDs (at least 16 hits — one in definition + one in traceability each), a "Manual Grouping-First UX (Phase 9)" subsection exists, and the Coverage footer reads "16 total" for Product UX requirements.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fill in Phase 9 Goal + Success Criteria in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Find the `### Phase 9: Manual Grouping-First UX` section (currently near line 165). Replace its entire body with a structure matching the Phase 7/8 format:

```
### Phase 9: Manual Grouping-First UX
**Goal**: Make manual drag-and-drop the primary grouping UX — fast and frictionless enough that AI auto-sort is an optional secondary button, not the default. Staged groups in IndexedDB are promoted to Supabase as product records one group at a time via the existing POST /api/images/upload groupAsOne=true path, with per-group failure isolation so a single upload error never wipes the user's staging work. The free-text AI prompt and preset group-size controls are permanently removed from the staging UI.
**Depends on**: Phase 8
**Requirements**: GROUP-05, GROUP-06, GROUP-07, GROUP-08, GROUP-09, GROUP-10, GROUP-11, GROUP-12
**Success Criteria** (what must be TRUE):
  1. On file drop, the user lands directly in manual drag-and-drop mode — no three-card mode chooser appears; AI auto-sort is only reachable as a secondary toolbar button
  2. The "Custom AI Prompt" textarea, brand-tone selector, per-group maxImages +/- controls, and [1..5] presets toolbar are gone from upload-zone.tsx
  3. Clicking a thumbnail selects only that one; Shift-click extends a range in visual order across groups; Cmd/Ctrl-click toggles a single thumbnail; selection persists through drag so multi-select batch moves work
  4. Dropping a dragged thumbnail onto an invalid target (page background, over === null) animates it back to its origin instead of vanishing
  5. Groups with more than 20 items show an amber "Large group (N) — consider splitting" badge but adding more items is never blocked
  6. Clicking "Confirm" uploads each group with POST /api/images/upload groupAsOne=true (parallelism capped at 2); successful groups have their IDB blobs removed; failed groups remain in the grid with a red "Retry" button and the user can click to retry without losing work
  7. A "+ New group" drop target is always visible at the end of the grid and accepts dropped thumbnails to create a new empty group
**Plans**: 5 plans
Plans:
- [ ] 09-01-PLAN.md — Define GROUP-05..12 in REQUIREMENTS.md and fill Phase 9 Goal + Success Criteria in ROADMAP.md
- [ ] 09-02-PLAN.md — Delete prompt UI, presets, mode chooser, maxImages controls, chunkArray, TONES from upload-zone.tsx
- [ ] 09-03-PLAN.md — Add useGroupSelection hook (Shift/Cmd-click) + fix snap-back dropAnimation timing
- [ ] 09-04-PLAN.md — Per-file landing (one group per file), soft large-group warning (threshold 20), verify always-visible "+ New group"
- [ ] 09-05-PLAN.md — Per-group failure-isolated handleConfirm rewrite with inline retry + human verification checkpoint
```

Leave the "Details:" paragraph below intact (or remove it — it is now redundant with the filled Goal). Also update the progress table at the bottom to show `| 9. Manual Grouping-First UX | 0/5 | Not started | - |`.
  </action>
  <verify>
    <automated>grep -cE "GROUP-(0[5-9]|1[0-2])" .planning/ROADMAP.md</automated>
  </verify>
  <done>
ROADMAP.md Phase 9 section has a concrete Goal (not `[To be planned]`), a Requirements line listing GROUP-05..12, 7 Success Criteria, 5 plans listed, and no stray `TBD (run /gsd:plan-phase 9 ...)` placeholder.
  </done>
</task>

</tasks>

<verification>
- REQUIREMENTS.md: `grep -E "GROUP-1[0-2]" .planning/REQUIREMENTS.md` returns at least 6 matches
- ROADMAP.md: `grep -A2 "Phase 9: Manual Grouping" .planning/ROADMAP.md | grep Goal` does NOT contain `[To be planned]`
- Both files parse as valid markdown (no broken tables)
</verification>

<success_criteria>
1. All eight GROUP-05..12 IDs defined in REQUIREMENTS.md Phase 9 subsection
2. All eight IDs present in the REQUIREMENTS.md Traceability table mapped to Phase 9
3. ROADMAP.md Phase 9 Goal is concrete prose (not a placeholder)
4. ROADMAP.md Phase 9 lists 5 plans matching the names used by plans 02-05 of this phase
</success_criteria>

<output>
After completion, create `.planning/phases/09-manual-grouping-first-ux/09-01-SUMMARY.md` following the summary template.
</output>
