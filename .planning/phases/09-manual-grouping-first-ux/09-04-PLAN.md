---
phase: 09-manual-grouping-first-ux
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - client/src/components/upload-zone.tsx
autonomous: true
requirements:
  - GROUP-08
  - GROUP-12
must_haves:
  truths:
    - "A group containing more than 20 items renders an amber 'Large group (N) — consider splitting' advisory badge in its header"
    - "Adding items to a large group is never blocked — badge is advisory only"
    - "A '+ New group' drop target is rendered at the end of the group grid whenever groups.length > 0, and it accepts dropped thumbnails to create a new group"
  artifacts:
    - path: "client/src/components/upload-zone.tsx"
      provides: "LARGE_GROUP_THRESHOLD constant + soft-warning badge in DroppableGroup header; verified DroppableNewGroup placement"
      contains: "LARGE_GROUP_THRESHOLD"
  key_links:
    - from: "DroppableGroup header"
      to: "group.items.length > LARGE_GROUP_THRESHOLD"
      via: "conditional badge render"
      pattern: "LARGE_GROUP_THRESHOLD"
---

<objective>
Add the soft large-group advisory and confirm the always-visible "+ New group" drop target is in place. Both are small, additive edits within upload-zone.tsx.

Purpose: GROUP-08 (no hard cap, soft warning at 20) and GROUP-12 (discoverable empty-group creation). These close the final two pure-UX requirements before the promotion-rewrite in Plan 05.
Output: One edited file.
</objective>

<execution_context>
@/Users/lefterisgilmaz/Desktop/lisai-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lefterisgilmaz/Desktop/lisai-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-manual-grouping-first-ux/09-RESEARCH.md
@client/src/components/upload-zone.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add LARGE_GROUP_THRESHOLD badge + verify DroppableNewGroup always visible</name>
  <files>client/src/components/upload-zone.tsx</files>
  <action>
**Edit A — Soft large-group warning (GROUP-08):**

1. At the top of upload-zone.tsx, near other constants, add: `const LARGE_GROUP_THRESHOLD = 20;`
2. Inside the `DroppableGroup` component (or the inline group-card render), in the group header area (next to the group title or item count), add a conditional badge:
```tsx
{group.items.length > LARGE_GROUP_THRESHOLD && (
  <span
    className="ml-2 inline-flex items-center rounded-full bg-amber-100 px-2 py-0.5 text-xs font-medium text-amber-900"
    title="Large group — consider splitting into multiple products"
    data-testid={`large-group-warning-${group.id}`}
  >
    Large group ({group.items.length}) — consider splitting
  </span>
)}
```
3. The badge MUST NOT block interaction. Do not disable the drop target, do not cap the add, do not wrap with `pointer-events: none`. It is purely advisory.
4. Apply the exact same badge to both the manual path AND to groups produced by auto-sort (RESEARCH Pitfall 6 says "show it universally as a non-dismissible badge"). Since we render a single DroppableGroup component for both, a single edit covers both.

**Edit B — Verify DroppableNewGroup is always rendered (GROUP-12):**

1. Locate the JSX that renders DroppableNewGroup (from Plan 02 it should still exist, anchored after the group grid around line 881 pre-deletion, or wherever it landed post-deletion).
2. Confirm it is rendered unconditionally when `groups.length > 0`. It MUST NOT be hidden behind `mode === ...` (that state is gone) or any other conditional except possibly "hide during active upload".
3. If it was accidentally wrapped in a deleted conditional during Plan 02, re-parent it so it is always rendered after the last group card.
4. Add a `data-testid="droppable-new-group"` attribute on its root element for UAT visibility.
5. Confirm the drop handler in `handleDragEnd` already creates a new empty group when `over.id === "new-group"` (or whatever sentinel the existing DroppableNewGroup uses). If missing, wire it: create a new Group with a fresh UUID containing the dragged items, append to `groups`, remove them from their origin group, persist via `saveGroups`.

Implements GROUP-08 and GROUP-12.
  </action>
  <verify>
    <automated>cd /Users/lefterisgilmaz/Desktop/lisai-app && grep -q "LARGE_GROUP_THRESHOLD" client/src/components/upload-zone.tsx && grep -q "DroppableNewGroup" client/src/components/upload-zone.tsx && grep -q "droppable-new-group" client/src/components/upload-zone.tsx && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
`LARGE_GROUP_THRESHOLD = 20` is defined; a conditional amber badge renders in the group header when items.length > threshold with no user interaction blocked; DroppableNewGroup renders unconditionally when groups.length > 0; `data-testid="droppable-new-group"` is set; `npm run build` passes.
  </done>
</task>

</tasks>

<verification>
- `grep -c "LARGE_GROUP_THRESHOLD\|droppable-new-group\|large-group-warning" client/src/components/upload-zone.tsx` >= 3
- `npm run build` succeeds
- No new TypeScript errors
</verification>

<success_criteria>
1. Large groups (>20 items) show an advisory badge; small groups do not
2. Adding items to a large group still works (no cap)
3. DroppableNewGroup is always visible when there is at least one group
4. Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/09-manual-grouping-first-ux/09-04-SUMMARY.md` following the summary template.
</output>
